<Window x:Class="Chess_UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Chess_UI"
        mc:Ignorable="d"
        Title="MainWindow" Height="600" Width="600"
        Icon="/Assets/icon.ico">
    <Grid>
        <Grid.Background>
            <ImageBrush ImageSource="/Assets/Board.png"></ImageBrush>
        </Grid.Background>
        
        <UniformGrid x:Name="HighlightGrid" Rows="8" Columns="8" IsHitTestVisible="False">
        </UniformGrid>

        <UniformGrid x:Name="PieceGrid" Rows="8" Columns="8" Background="Transparent" MouseDown="BoardGrid_MouseDown">
        </UniformGrid>

        <Grid x:Name="GameOverMenu" Background="#AA000000" Visibility="Hidden">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock x:Name="WinnerText" Foreground="White" FontSize="36" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>
                <Button Content="RESTART" FontSize="24" Padding="20,10" Click="Restart_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
